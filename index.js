import React from 'react';
import ReactDOM from 'react-dom';
import ShootingStarsApp from './ShootingStarsApp';

ReactDOM.render(
    <React.StrictMode>
        <ShootingStarsApp />
    </React.StrictMode>,
    document.getElementById('root')
);
